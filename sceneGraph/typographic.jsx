
// Typographic

export const Font = (props) => (
  <div class='Font'>
    Font: {props}
  </div>
)

export const ScrollingLabel = (props) => (
  <div class='ScrollingLabel'>
    ScrollingLabel: {props}
  </div>
)

export const ScrollableText = (props) => (
  <div class='ScrollableText'>
    ScrollableText: {props}
  </div>
)
